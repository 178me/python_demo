#  import os
#  self.ld.list2 = os.popen(f"{self.ld.exec_path} list2").read().split("\n")
#  self.ld.list2.pop()
#  for index in range(len(self.ld.list2)):
    #  self.ld.list2[index] = self.ld.list2[index].split(",")
#  log.info(self.ld.list2)
#  log.info("获取模拟器状态成功")
